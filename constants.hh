#pragma once
namespace constants {
  constexpr int SCREEN_WIDTH{ 640 };
  constexpr int SCREEN_HEIGHT{ 480 };
  constexpr int SCREEN_FPS{ 24 };
  constexpr int SCREEN_TICKS_PER_FRAME{ 1000 / SCREEN_FPS };
} 